root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/scheduler ./cmd/scheduler"
bin = "./tmp/scheduler"
delay = 500
exclude_dir = ["tmp", "docker", "docs"]
include_ext = ["go"]

[log]
time = true
